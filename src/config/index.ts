import router from './router';
import { categoryConfig } from './category';
import { Banners } from './bannerAdvertisement';
import * as PreviewProduct from './previewProduct';

const config = {
    router,
    categoryConfig,
    Banners,
    PreviewProduct,
};

export default config;
